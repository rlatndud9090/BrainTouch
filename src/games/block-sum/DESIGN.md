# 🧱 블록셈 (Block Sum) 설계 문서

> 일본 전통놀이 '다루마 오토시'에서 영감을 받은 덧셈 퍼즐 게임

---

## 📌 게임 개요

| 항목          | 내용                             |
| ------------- | -------------------------------- |
| **게임 ID**   | `block-sum`                      |
| **장르**      | 퍼즐 / 두뇌 게임                 |
| **조작**      | 터치 + 왼쪽 스와이프             |
| **제한 시간** | 60초                             |
| **목표**      | 블록을 제거하여 목표 숫자 만들기 |

---

## 🎮 게임 규칙

### 기본 규칙

1. 숫자가 쓰인 블록 4~6개가 탑처럼 세로로 쌓여 등장
2. 화면에 **목표 숫자**가 제시됨
3. 블록을 터치 후 **왼쪽으로 스와이프**하면 블록 제거
4. **남은 블록들의 합 = 목표 숫자**가 되면 **확인 버튼** 터치
5. 성공하면 점수 획득, 다음 라운드 진행
6. 60초 내에 최대한 많은 라운드 클리어!

### 실패 조건

- 블록을 너무 많이 제거해서 **목표 달성이 불가능**해지면 자동 실패
- 실패 시 다음 라운드로 넘어감 (점수 없음)

### 점수 시스템

```
점수 = 100 × 남은 블록 수 × 난이도 보너스

난이도 보너스:
- 하 (Easy): ×1
- 중 (Normal): ×2
- 상 (Hard): ×3

예시: 상 난이도에서 4개 블록 남기고 성공
      → 100 × 4 × 3 = 1200점
```

---

## 📊 난이도 시스템

| 난이도      | 블록 개수 | 숫자 범위 | 승급 조건          |
| ----------- | --------- | --------- | ------------------ |
| 하 (Easy)   | 4개       | 1~9       | 연속 3회 성공 → 중 |
| 중 (Normal) | 5개       | 1~12      | 연속 3회 성공 → 상 |
| 상 (Hard)   | 6개       | 1~15      | 유지               |

- 실패 시 연속 성공 횟수 초기화 (난이도는 유지)

---

## 🖼️ UI 레이아웃

```
┌─────────────────────────────────┐
│ 60.0         0점    난이도: 하  │  ← HUD
├─────────────────────────────────┤
│            목표                 │
│            [ 15 ]               │  ← 목표 숫자
│          현재: 24               │  ← 현재 합계
├─────────────────────────────────┤
│         ┌───────┐               │
│         │   7   │  ← 스와이프   │
│         ├───────┤               │
│         │   5   │               │
│         ├───────┤               │  ← 블록 탑
│         │   8   │               │
│         ├───────┤               │
│         │   4   │               │
│         └───────┘               │
├─────────────────────────────────┤
│         [ 확인 ]                │  ← 확인 버튼
└─────────────────────────────────┘
```

---

## 🔧 기술 구현

### 파일 구조

```
src/games/block-sum/
├── config.ts              # Phaser 게임 설정
├── DESIGN.md              # 이 문서
├── scenes/
│   ├── GameScene.ts       # 메인 게임 씬
│   └── ResultScene.ts     # 결과 화면
└── utils/
    └── BlockGenerator.ts  # 블록 생성 알고리즘
```

### 핵심 알고리즘

#### 목표 숫자 생성

1. 난이도에 맞는 블록 생성 (랜덤 숫자)
2. 모든 부분집합의 합 계산 (비트마스크, 최대 2^6 = 64개)
3. 가능한 합 중 랜덤 선택 → 목표 숫자로 설정

```typescript
// 모든 가능한 합 계산
for (let mask = 0; mask < 1 << n; mask++) {
  // mask 비트가 1인 블록은 제거, 0인 블록은 유지
  // 유지된 블록들의 합 계산
}
```

#### 달성 가능 여부 체크

- 블록 제거 시마다 남은 블록으로 목표 달성 가능한지 검사
- 불가능하면 즉시 실패 처리

### 스와이프 감지

```typescript
// pointerdown에서 시작 위치 저장
// pointerup에서 이동 거리 계산
const dx = pointer.x - startX;
if (dx < -50 && Math.abs(dy) < Math.abs(dx)) {
  // 왼쪽 스와이프 → 블록 제거
}
```

### 낙하 애니메이션

- 블록 제거 시 Tween으로 부드러운 낙하
- `Bounce.easeOut` 이징으로 자연스러운 착지

---

## 🎨 디자인 가이드

### 색상

- **테마 색상**: 노란 계열 (`#ffc947`)
- **블록 배경**: 어두운 보라 (`#2a2a4e`)
- **성공 피드백**: 녹색 (`#4ecca3`)
- **실패 피드백**: 빨강 (`#e94560`)

### 애니메이션

| 이벤트    | 애니메이션                     |
| --------- | ------------------------------ |
| 블록 제거 | 왼쪽으로 슬라이드 아웃 (200ms) |
| 블록 낙하 | 바운스 이징 (150ms)            |
| 성공      | 점수 텍스트 팝업               |
| 실패      | 화면 흔들림 + "불가능!" 텍스트 |
| 난이도 업 | "🔥 난이도 UP!" 텍스트         |

---

## 📝 개발 히스토리

| 날짜       | 내용            |
| ---------- | --------------- |
| 2025-12-19 | 블록셈 MVP 구현 |

---

_작성일: 2025-12-19_
